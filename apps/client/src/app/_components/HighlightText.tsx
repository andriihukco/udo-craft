"use client";
 
import React from "react";
 
/**
 * Simplified component that just renders children.
 * Highlights were removed as per user request.
 */
export function RoughHighlight({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
 
export function HighlightText({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
 
export function UnderlineText({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
